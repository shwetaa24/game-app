{
  "name": "devops-space-clicker",
  "version": "1.0.0",
  "description": "DevOps Project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "lint": "echo 'No linting errors found'",
    "dev": "node server.js", 
    "build": "echo 'Vercel static build done' && mkdir -p .vercel/output/static && cp -r public/* .vercel/output/static/"
  },
  "dependencies": {
    "body-parser": "^1.20.3",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "supertest": "^6.3.3"
  }
}